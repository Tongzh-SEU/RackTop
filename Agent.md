## 0 固定工作区与工具路径

RackTop 开发 Agent 必须优先使用以下固定路径，不得仅依赖当前 shell 的 `PATH` 或工作目录推断工具是否存在：

- RackTop 仓库根目录：`/Users/tong_zh/Library/Mobile Documents/com~apple~CloudDocs/ZHT桐桐/RackTop`；执行项目命令、读取版本信息、检查 Git 状态和生成安装包时，应显式将该目录作为工作目录。
- GitHub CLI：`/Users/tong_zh/.local/bin/gh`；该用户级安装会跨终端和 Codex 会话保留。
- 当前 shell 的 `PATH` 可能不包含 `~/.local/bin`。`command -v gh` 没有输出时，必须继续检查 `/Users/tong_zh/.local/bin/gh` 是否为可执行文件；文件存在时直接使用绝对路径，不得误报未安装或重复安装。
- GitHub CLI 二进制与登录状态相互独立。使用前通过 `/Users/tong_zh/.local/bin/gh auth status` 检查系统钥匙串中的认证；认证失效时只重新登录，不重复安装二进制。

## 1 UI、动画与交互设计技能

RackTop 的网页界面以及 Tauri WebView 中的桌面界面，在进行页面设计、组件实现、动画设计和交互体验优化时，统一优先使用 [`emilkowalski/skills`](https://github.com/emilkowalski/skills) 作为设计工程技能集。

该技能集用于辅助以下工作：

- 页面布局、视觉层级和界面细节设计；
- 组件状态、反馈和交互行为设计；
- 动画时机、缓动、持续时间和空间关系设计；
- 识别真正需要动画的交互位置，避免无意义动效；
- 审查并改进已有动画；
- 选择适合项目的 UI 组件库；
- 结合 Apple 设计原则优化 macOS 体验；
- 保证 Windows 与 macOS 上的交互语义一致。

### 技能检查与主动安装

开始任何 UI、组件、动画或交互体验任务前，开发 Agent 应先检查当前项目是否已经安装 `emilkowalski/skills`。

如果未安装，应主动在项目根目录执行：

```bash
npx skills@latest add emilkowalski/skills
```

安装失败时，应明确报告原因和缺失条件，不得假装技能已经生效。只有在无法完成安装时，才可以暂时使用项目现有设计规范继续开发，并在交付说明中记录该降级情况。

该技能属于开发阶段的 Agent Skills，不应被打包为 RackTop 用户端运行依赖。最终用户安装 RackTop 时不需要安装 Node.js、`skills` CLI 或该技能仓库。

### 按任务调用对应技能

在技能可用时，应根据任务调用合适的子技能：

- `emil-design-eng`：页面、组件、交互和整体设计工程实现；
- `pick-ui-library`：选择维护良好且适合任务的 UI 组件库；
- `find-animation-opportunities`：识别适合增加动效的位置，以及不应添加动效的位置；
- `animation-vocabulary`：准确描述需要的动画行为；
- `review-animations`：严格审查已经实现的动画；
- `improve-animations`：输出按优先级排列的动画改进方案；
- `apple-design`：参考 Apple 的界面与流畅动效原则优化 macOS 体验。

不能因为安装了技能就机械地给所有操作增加动画。监控界面的首要目标仍然是信息清晰、状态稳定、数据易读和操作可预测。

### UI 工作流程

每个主要页面或交互功能建议遵循以下流程：

1. 使用 `emil-design-eng` 明确信息层级、组件结构和交互状态；
2. 必要时使用 `pick-ui-library` 选择成熟组件，而不是重复手写基础组件；
3. 使用 `find-animation-opportunities` 判断哪些状态变化真正需要动效；
4. 实现页面、组件和动画；
5. 使用 `review-animations` 审查进入、退出、切换、展开和反馈动画；
6. 使用 `improve-animations` 生成并执行高优先级改进项；
7. 在 macOS 版本上使用 `apple-design` 做一次平台体验检查；
8. 检查减少动态效果、键盘操作、焦点状态和无障碍支持。

### RackTop 动效原则

RackTop 是高信息密度的实时监控工具，动效应遵循以下原则：

- 动画服务于状态变化和空间关系，不作为装饰；
- 实时图表更新应平滑，但不得造成数据延迟或视觉误导；
- 服务器切换和右侧详情菜单切换应保持上下文连续；
- 告警、离线和认证风险必须清晰，不依赖夸张动画吸引注意；
- 当前用户任务的强调色变化应稳定，避免持续闪烁；
- 数据高频刷新时，不应让卡片整体反复位移或缩放；
- 支持系统“减少动态效果”设置，并提供应用内关闭非必要动画的选项；
- 动画不能阻塞 SSH 连接、指标刷新、错误反馈或关键操作。

## 2 需求完成后的版本管理与 GitHub 交付工作流

每当完成一个独立、可交付的需求，包括新增功能、Bug 修复、功能优化、重构、兼容性调整或文档变更，开发 Agent 必须同步完成版本号计算、版本说明更新、验证、Git 提交以及 GitHub 上传。不得只修改代码而遗漏版本信息，也不得在交付完成后把版本记录延迟到下一次需求。

仅修改 `Agent.md` 中的 Agent 工作规范属于开发流程维护，不计入 RackTop 产品版本，不修改 npm、Cargo、Tauri 版本号，也不新增 `docs/VERSION_INFOS.md` 产品版本条目；仍应提交并推送该规范变更。

### 完成标准

一个需求只有在以下事项全部完成后，才可以视为交付完成：

1. 已完成需求范围内的代码、测试、配置和文档修改；
2. 已执行适用的检查、测试和构建，或明确记录未执行原因；
3. 已根据当前最新版本计算新版本号；
4. 已更新 `docs/VERSION_INFOS.md`；
5. 已创建包含当前版本号的 Git 提交；
6. 已将提交推送到 RackTop GitHub 仓库；
7. 项目采用 Pull Request 流程时，已创建或更新对应 PR；
8. 已在最终交付说明中报告版本、提交、分支、推送目标、PR 和验证结果。

如果因为 GitHub 鉴权、仓库未授权、远程地址错误、网络、分支保护、权限不足、测试失败或其他客观原因无法完成提交或上传，必须明确报告真实阻塞原因，不得声称已经提交、推送或创建 PR。

### 版本号规则

RackTop 使用 `主版本.次版本.修订版本`（`x.y.z`）格式。

- **Bug 修复**：递增修订版本号，即 `x.y.z → x.y.(z+1)`。
  - 示例：`0.1.1 → 0.1.2`
- **新增功能**：递增次版本号，并将修订版本号归零，即 `x.y.z → x.(y+1).0`。
  - 示例：`0.1.2 → 0.2.0`
- **功能与修复同时存在**：只要本次包含用户可见的新功能，就按功能版本处理，并在同一版本说明中分别列出新增、优化和修复内容。
- **重大不兼容变更**：只有用户或项目发布计划明确要求时，才递增主版本号；开发 Agent 不得自行决定主版本升级。
- 如果 `docs/VERSION_INFOS.md` 尚不存在，首次功能版本使用 `0.1.0`，首次纯 Bug 修复版本使用 `0.0.1`。
- 计算版本号前必须读取当前最新版本，不得固定写成 `x.x.1` 或 `x.1.x`，也不得重复使用已经存在的版本号。

### 版本说明文件

所有版本信息统一维护在：

```text
docs/VERSION_INFOS.md
```

该文件是面向用户、测试人员和发布维护者的正式版本说明。更新时必须遵守以下规则：

- **最新版本始终位于最上方**：新版本章节应插入到文档标题和简介之后、上一版本之前。
- **不得覆盖历史版本**：禁止删除、覆盖、压缩、合并或改写已有版本记录；只能新增当前版本章节，并将历史版本完整保留在其下方。
- 每个版本必须记录实际修改时间和发布日期。
- 修改时间使用 `YYYY-MM-DD HH:mm:ss ±HH:MM` 格式，必须包含时区；没有其他项目约定时使用当前本地时区。
- 版本说明必须依据本次真实修改和实际验证结果编写，不得编造未实现的功能、未运行的测试、未构建的平台或未完成的发布结果。
- 没有内容的可选章节可以省略，但“版本概述”“本版本修改”和“验证记录”不得省略。

### 版本条目格式

每个版本按以下详细结构编写，并根据实际变更选择适用章节：

```markdown
## X.Y.Z — 版本主题

- 修改时间：YYYY-MM-DD HH:mm:ss ±HH:MM
- 发布日期：YYYY-MM-DD
- 发布阶段：功能版本 / Bugfix / MVP / 内部预览 / 正式发布
- 桌面平台：macOS、Windows
- 升级类型：说明是否兼容以及是否需要迁移数据

### 版本概述

说明本版本解决的问题、主要目标以及对用户的影响。

### 新增功能

- 用户可见的新能力；
- 新增的页面、组件、配置或工作流。

### 优化内容

- 交互、性能、可访问性、显示效果或开发体验改进。

### 修复内容

- 已修复的问题；
- 问题发生条件及修复后的行为。

### 保持不变的行为

- 容易引起误解但本次没有改变的产品语义、数据格式或兼容行为。

### 验证记录

- 实际运行的前端测试、Rust 测试、类型检查和生产构建；
- 实际验证的平台、服务器、GPU 型号和关键场景；
- 未执行的验证必须说明原因，不得写成已经通过。

### 已知事项

- 尚未完成、仍需确认或依赖真实发布环境验证的问题。

### 升级说明

说明是否可以直接升级、是否需要迁移数据，以及配置和历史数据是否兼容。
```

章节使用规则：

- 纯功能版本可以省略“修复内容”；
- 纯 Bug 修复版本可以省略“新增功能”；
- 不涉及行为兼容说明时可以省略“保持不变的行为”；
- 没有已知问题时，应写明“当前未发现新增已知事项”，不得保留空标题；
- 仅文档或工程维护变更也必须说明对用户或开发流程的实际影响。

### Git 提交说明

完成需求并更新 `docs/VERSION_INFOS.md` 后，Git 提交说明必须包含本次当前版本号。

统一格式：

```text
vX.Y.Z <type>: <变更摘要>
```

推荐使用以下 `type`：

- `feat`：新增功能；
- `fix`：Bug 修复；
- `refactor`：不改变用户功能的重构；
- `perf`：性能优化；
- `docs`：仅修改文档；
- `test`：仅新增或调整测试；
- `chore`：构建、依赖或工程维护。

示例：

```text
v0.1.2 fix: 修复服务器删除后的快照缓存残留
```

```text
v0.2.0 feat: 增加 GPU 告警规则管理
```

提交正文应在需要时补充：

- 版本号和变更类型；
- 修改内容与原因；
- 用户或开发者影响；
- 实际验证结果；
- 已知限制或未执行的验证。

版本说明必须与对应代码、测试和文档放在同一个需求提交中，不得先提交代码后遗漏版本记录，也不得把无关修改混入同一提交。

### GitHub 提交与上传流程

需求完成后，开发 Agent 必须使用 GitHub 技能和可用的 Git 工具，将本次修改上传到 RackTop GitHub 仓库。

1. 检查 `git status`、当前分支、远程仓库和完整差异，确认仓库确实是 RackTop。
2. 只暂存当前需求相关的源代码、测试、配置、文档以及 `docs/VERSION_INFOS.md`，不得静默夹带无关修改。
3. 如果当前位于默认分支，应按项目约定创建独立需求分支，例如：

   ```text
   agent/v0.2.0-gpu-alert-rules
   ```

4. 使用包含版本号的提交说明创建 Git 提交。
5. 将当前分支推送到 RackTop 的远程 GitHub 仓库，并设置上游分支。
6. 项目采用 Pull Request 流程时，应创建或更新对应 PR；默认创建 Draft PR，除非用户明确要求直接提交或标记为可审查。
7. PR 标题必须包含版本号，格式可与提交说明保持一致，例如：

   ```text
   v0.2.0 feat: 增加 GPU 告警规则管理
   ```

8. PR 说明至少应包含当前版本号、修改内容、修改原因、影响范围、验证结果和已知限制。
9. 上传后核对远程分支、提交哈希和 PR 信息，确认上传结果真实存在。

不得因为本地修改已经完成就省略 GitHub 上传。若 GitHub 技能不可用或仓库未授权，应停止虚假的上传声明，保留本地修改，并在交付说明中给出具体授权或修复步骤。

### 最终交付报告

每次完成需求后，最终回复必须报告：

- 本次版本号；
- 变更类型；
- 修改时间；
- Git 分支；
- Git 提交说明；
- 推送的远程仓库与远程分支；
- Pull Request 地址或直接推送结果；
- 已执行的测试、检查和构建；
- 未执行的验证及原因；
- 仍然存在的阻塞或已知事项。

任何未实际完成的步骤都必须明确标注为未完成，不得使用模糊措辞让用户误以为已经提交、上传、测试或发布。
